import 'package:flutter/material.dart';

class VFirst extends StatelessWidget{
  const VFirst({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return const Scaffold(
      body: Center(
        child: Text('Welcome!'),
      ),
    );
  }
}